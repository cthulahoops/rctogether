[project]
name = "rctogether"
version = "0.3.3"
description = ""
authors = [{name = "Adam Kelly", email = "adam@cthulahoops.org"}]
requires-python = ">=3.8"
dependencies = [
    "aiohttp>=3.7.4",
    "websockets>=10.2",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.1",
    "pytest-asyncio>=0.15.1",
    "black>=21.6b0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
